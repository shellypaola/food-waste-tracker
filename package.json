{
  "name": "freshkeeper",
  "version": "1.0.0",
  "description": "Smart food waste prevention app",
  "main": "index.html",
  "dependencies": {
    "@vercel/kv": "^1.0.1",
    "web-push": "^3.6.6"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "deploy": "vercel --prod"
  }
}